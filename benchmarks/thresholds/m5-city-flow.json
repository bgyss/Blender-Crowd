{
  "schema_version": 1,
  "scene": "m5_city_flow",
  "basis": "Set 2026-08-14 from the measured schema-v5 per-tier baselines at 1,000 and 10,000 agents on Apple M1 Max (see docs/benchmarks/2026-08-14-m5-10k.md). Each limit is a bar with headroom over the worse of the two scales, not a restatement of either; the rejected fixed-lane cadence candidates in 2026-08-13-m5-10k-failed-baseline.md fail these limits.",
  "declared_profile_mode": "stable_agent_id_hash_target_10_percent_s1_90_percent_s2",
  "declared_background_share": 0.9,
  "declared_share_tolerance": 0.02,
  "s2_update_interval_ticks": 2,
  "min_ticks_per_second": 10.0,
  "min_completion_rate": 1.0,
  "required_tiers": ["S1", "S2"],
  "tiers": {
    "S1": {
      "min_completion_rate": 1.0,
      "max_penetration_agent_ticks_per_agent_tick": 0.000001,
      "max_penetration_depth_m": 0.02,
      "max_stalled_agent_share": 0.2,
      "max_stall_agent_ticks_per_agent_tick": 0.005,
      "max_heading_reversals_per_agent_tick": 0.15,
      "max_abrupt_turns_per_agent_tick": 0.00002,
      "max_animation_evaluation_share": 1.0
    },
    "S2": {
      "min_completion_rate": 1.0,
      "max_penetration_agent_ticks_per_agent_tick": 0.00001,
      "max_penetration_depth_m": 0.05,
      "max_stalled_agent_share": 0.2,
      "max_stall_agent_ticks_per_agent_tick": 0.005,
      "max_heading_reversals_per_agent_tick": 0.15,
      "max_abrupt_turns_per_agent_tick": 0.00002,
      "max_animation_evaluation_share": 0.75
    }
  }
}
